# BeatWizard Dependencies - Render.com Optimized
# Lighter version to avoid memory/build timeout issues

# Core Web Framework
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Basic Utilities  
requests>=2.31.0

# Core Audio Analysis (Essential)
numpy==1.26.4
soundfile==0.12.1
scipy==1.11.4 ; python_version < '3.13'
librosa==0.10.1
pyloudnorm==0.1.1
audioread>=3.0.0

# Lightweight NumJit (reduced version)
numba==0.58.1
llvmlite==0.41.1

# MP3 support
pydub>=0.25.1

# Configuration & Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.7.0

# Machine Learning (Essential only - remove scikit-learn temporarily)
# scikit-learn>=1.0.0  # Commented out to reduce build size

# Install BeatWizard package itself
-e .
